pipeline {
     agent any 
     stages {
         stage('Build') {
             steps {
                 parallel linux: {
                     node('master') {
                         checkout scm
                         sh 'qmake src/ide.pro && make'
                     }
                 },
                 windows: {
                     node('windows') {
                         checkout scm
                         bat 'cd src && qmake ide.pro && make'
                     }
                 }
             }
         }
     }
}
