pipeline {
     agent none 
     stages {
         stage('Build') {
             parallel linux: {
                 node('master') {
                     checkout scm
                 }
             },
             windows: {
                 node('windows') {
                     checkout scm
                 }
             }
         }
     }
}
