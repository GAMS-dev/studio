pipeline {
     agent any 
     stages {
         stage('Build') {
             steps {
                 parallel linux: {
                     node('master') {
                         checkout scm
                         sh 'qmake src/ide.pro'
                     }
                 },
                 windows: {
                     node('windows') {
                         checkout scm
                     }
                 }
             }
         }
     }
}
