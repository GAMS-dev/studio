pipeline {
     agent any 
     stages {
         stage('Build') {
             steps {
                 parallel linux: {
                     node('master') {
                         checkout scm
                     }
                 },
                 windows: {
                     node('windows') {
                         checkout scm
                     }
                 }
             }
         }
     }
}
