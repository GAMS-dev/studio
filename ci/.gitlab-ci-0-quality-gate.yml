quality-gate:
  stage: quality
  tags: [linux]
  image:
    name: $GAMS_CONTAINER_REGISTRY/qt-machines/leg/analyzer-${QT_IMAGE_VERSION}:latest
    entrypoint: [""]   # prevent startup.sh
  script:
    - GIT_TERMINAL_PROMPT=0 SKIP=end-of-file-fixer pre-commit run -a
